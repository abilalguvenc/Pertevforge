// Firebase yapılandırması
const firebaseConfig = {
    // Firebase console'dan alınacak config bilgileri
};

// Firebase'i başlat
firebase.initializeApp(firebaseConfig);
const db = firebase.firestore(); 